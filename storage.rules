rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /branding/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    match /ads/{allPaths=**} {
      allow read: if true;
    }
  }
}